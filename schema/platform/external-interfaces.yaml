external_interfaces: map(include('external_interface'))

---
external_interface:
  targets: list(include('external_interface_target'))

---
external_interface_target:
  application: str()
  endpoint: str()