policies:

  allow-dmz-to-internal:
    description: Allow DMZ traffic to Internal network
    from: dmz
    to: internal
    action: allow

  allow-internal-to-database:
    description: Allow Internal traffic to Database network
    from: internal
    to: database
    action: allow
